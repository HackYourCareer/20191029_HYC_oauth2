# This is resource server form oAuth 2.0
It has hardcoded URL's for authorization server in `Oauth2ResourceServerConfigurerAdapter` class.

# To run project
Run following command: `mvn spring-boot:run`